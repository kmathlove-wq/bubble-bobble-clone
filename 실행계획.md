# 버블보블 따라만들기 — 실행계획

> **작업 방식:** 아래 마일스톤을 위에서부터 하나씩. 각 마일스톤 끝에 "직접 확인" 목록을 통과하면
> 커밋하고 다음으로. 각 단계는 체크박스로 진행 추적.

**목표:** 브라우저에서 도는 버블보블 스타일 게임(30판+보스)을 순수 HTML+JS 캔버스로 만들고 GitHub Pages에 배포.

**구조:** 게임 엔진 없이 `requestAnimationFrame` 고정 timestep 루프. 개체(주인공/거품/적/아이템)는
각자 `update(dt)` / `draw(ctx)` 를 가진 평범한 클래스. 전역 네임스페이스 `window.BB` 하나에 모듈을 담음.
`index.html` 을 클래식 `<script>` 태그로 순서대로 로드 → 파일 더블클릭으로 실행 (file:// 에서 모듈 import는 CORS로 막힘).

**기술:** HTML5 Canvas 2D, WebAudio(효과음), localStorage(최고점수/음소거), gh CLI(배포).

**설계 문서:** `설계.md` (이 계획은 그 문서를 근거로 함 — 둘을 같이 읽을 것).

## 전역 제약 (모든 작업에 적용)

- 캔버스 내부 해상도 **512 x 384**, 타일 **16px**, 플레이 영역 **32 x 24 타일**. `image-rendering: pixelated`, 정수배 확대.
- 고정 timestep **1/60초(약 16.67ms)** 누적 방식. 한 프레임에 물리 스텝 최대 5회로 제한(스파이럴 방지).
- ES module import 금지. 클래식 `<script>`. 로드 순서: `util → assets → input → sfx → level → levels → player → bubble → enemy → item → hud → main`.
- 모든 코드/개체를 `window.BB` 아래에 둔다 (예: `BB.Player`, `BB.CONFIG`).
- 원작(타이토)의 스프라이트·음원·정확한 레벨 레이아웃 재현 금지. 사용자 제작 그림 + `배경음악.mp3` 만 사용. 효과음은 WebAudio 생성 또는 CC0.
- 원본 자료 폴더(`D:\민결이자료\...`)는 **읽기 전용**. `assets/` 로 복사만.
- 사용자 대상 문구는 쉬운 한국어. 개발용 별명·비유 금지.
- 화면 좌우 = 벽, 위아래 = 래핑(아래로 나가면 위에서 등장). 개체 전부 이 규칙 공유.
- 커밋 메시지 끝에:
  `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_011XQSSw8cBsRnYqjNkc7bAP`

## 사용자가 직접 정할 부분 (학습 기여)

- **M2**: 주인공 점프 느낌 — 중력 세기 / 점프 힘 / 최대 낙하속도 3개 숫자를 사용자가 골라 `CONFIG` 에 넣는다.
- **M8**: 30판 중 한 판(예: 7판)의 글자 지도를 사용자가 직접 그린다.

---

## M0: 준비 — 에셋 복사 + 저장소 뼈대

**파일:**
- 생성: `.gitignore`, `README.md`
- 복사: `assets/이미지/*.png`, `assets/음악/배경음악.mp3`

- [ ] **1.** `assets/이미지/`, `assets/음악/`, `assets/sfx/` 폴더 생성.
- [ ] **2.** 원본에서 복사:
  ```bash
  SRC="D:/민결이자료/코딩/엔트리/81강버블보블(인데 쓸모가 없...)"
  cp "$SRC/이미지/"*.png "assets/이미지/"
  cp "$SRC/효과음/배경음악.mp3" "assets/음악/배경음악.mp3"
  ```
- [ ] **3.** `.gitignore` 작성:
  ```
  .playwright-mcp/
  .DS_Store
  Thumbs.db
  *.tmp
  ```
- [ ] **4.** `README.md` 초안: 게임 소개 1줄, 조작법, 크레딧("만든 사람: 김민결 · 그림/음악: 김민결"),
  고지("팬이 공부용으로 만든 게임입니다. 버블보블은 타이토(TAITO)의 상표이며 이 프로젝트는 비상업·학습 목적입니다.").
- [ ] **5.** `git init` (프로젝트 폴더에서, 사용자 승인됨) → 첫 커밋 `chore: 에셋 복사 + 저장소 초기화`.

**직접 확인:** `assets/이미지/` 에 png 17개, `assets/음악/배경음악.mp3` 존재. `git log` 에 커밋 1개.

---

## M1: 뼈대 — 캔버스 + 게임 루프 + 상태 머신 + 에셋 로더

**파일:**
- 생성: `index.html`, `css/style.css`, `js/util.js`, `js/assets.js`, `js/input.js`, `js/main.js`

**인터페이스 (이후 작업이 의존):**
- `BB.CONFIG` — 게임 상수 객체 (`TILE:16`, `VW:512`, `VH:384`, `GRAVITY`, `JUMP_V`, `MAX_FALL`, `STEP:1/60` 등).
- `BB.util.aabb(a,b)` → bool. `a,b` 는 `{x,y,w,h}`.
- `BB.util.clamp(v,lo,hi)`, `BB.util.rand(lo,hi)`, `BB.util.randInt(lo,hi)`, `BB.util.choice(arr)`.
- `BB.util.wrapY(obj, vh)` — obj.y 가 화면 위/아래로 벗어나면 반대편으로.
- `BB.assets.images` — `{ '주인공1': HTMLImageElement, ... }` (파일명에서 확장자 뺀 키).
- `BB.assets.load(onProgress)` → Promise, 모든 이미지 + `배경음악.mp3`(Audio) 로드.
- `BB.input` — `BB.input.held('left'|'right'|'jump'|'bubble'|...)`, `BB.input.pressed(name)` (이번 프레임에 눌림), `BB.input.update()` (프레임 끝에 호출).
- `BB.game` — `{ state, score, hiScore, lives, round, setState(s) }`. state: `'BOOT'|'TITLE'|'PLAYING'|'ROUND_CLEAR'|'MISS'|'GAME_OVER'|'ENDING'`.

- [ ] **1.** `index.html`: `<canvas id="game" width="512" height="384">` + `<script>` 태그 12개(순서대로, 현재는 있는 것만) + 인라인 부팅 코드 없음(main.js가 담당).
- [ ] **2.** `css/style.css`: body 검정 배경 가운데 정렬, canvas `image-rendering:pixelated`, JS로 창 크기에 맞춰 정수배 `transform: scale()`.
- [ ] **3.** `js/util.js`: `BB` 네임스페이스 생성 + `BB.CONFIG` + 위 util 함수들. 각 함수에 한 줄 주석.
- [ ] **4.** `js/assets.js`: 이미지 목록 배열 → `new Image()` 로 로드, Promise.all. `배경음악.mp3` 는 `new Audio()`.
- [ ] **5.** `js/input.js`: `keydown`/`keyup` 리스너, 키맵(`ArrowLeft→left` 등, `Space→bubble`, `ArrowUp→jump`, `KeyZ/KeyX→bubble`, `KeyM→mute`, `KeyP→pause`), `held`/`pressed` 구현.
- [ ] **6.** `js/main.js`: 캔버스 컨텍스트, `BB.assets.load` → 완료 시 `BB.game.setState('TITLE')`. 고정 timestep 루프:
  ```js
  let acc = 0, last = performance.now();
  function frame(now){
    acc += Math.min((now-last)/1000, 0.25); last = now;
    let steps = 0;
    while (acc >= BB.CONFIG.STEP && steps < 5){ update(BB.CONFIG.STEP); acc -= BB.CONFIG.STEP; steps++; }
    render(ctx);
    BB.input.update();
    requestAnimationFrame(frame);
  }
  ```
  `update`/`render` 는 `BB.game.state` 로 분기. TITLE: "PUSH SPACE" 텍스트, Space 누르면 `PLAYING`(지금은 빈 화면).
- [ ] **7.** 로딩 화면: BOOT 상태에서 "로딩... N%" 표시.
- [ ] **8.** 커밋 `feat: 캔버스 + 게임 루프 + 에셋 로더 + 상태 머신`.

**직접 확인:** `index.html` 더블클릭 → 검정 화면에 "로딩" 잠깐 뜨고 "PUSH SPACE" 표시. Space 누르면 빈 플레이 화면. 콘솔 에러 없음.

---

## M2: 지형 + 주인공 이동

**파일:**
- 생성: `js/level.js`, `js/levels.js`, `js/player.js`
- 수정: `js/main.js` (PLAYING 에서 레벨·주인공 업데이트/그리기), `index.html` (script 추가)

**인터페이스:**
- `BB.parseLevel(def)` → `{ solids:[{x,y,w,h}], playerStart:{x,y}, enemySpawns:[{type,x,y}], time, theme }`. `def` 는 `{time,theme,map:[string]}`.
  `#` 연속을 가로로 병합해 사각형 수를 줄인다.
- `BB.LEVELS` — 배열. M2에서는 테스트용 1개(가운데 발판 몇 개).
- `BB.Level` 클래스: `new BB.Level(def)`, `.solids`, `.draw(ctx)` (테마 색 발판), `.collideDown(box, vy)` (한쪽 통행 발판 착지 판정 헬퍼).
- `BB.Player` 클래스: `new BB.Player(x,y)`, `.update(dt, level)`, `.draw(ctx)`, `.x .y .w .h .vx .vy .facing('L'|'R') .onGround`.

- [ ] **1.** 순수 로직 테스트 페이지 `test.html` 생성: `BB.parseLevel` 와 `BB.util.aabb` 를 간단한 `assert` 로 검사(pass/fail 콘솔+화면 출력). 프레임워크 없음.
- [ ] **2.** `test.html` 열어서 실패 확인(아직 `parseLevel` 없음).
- [ ] **3.** `js/level.js`: `BB.parseLevel` 구현(가로 병합 포함) + `BB.Level` 클래스.
- [ ] **4.** `test.html` 다시 열어 `parseLevel` 테스트 통과 확인:
  - 3x3 맵 `["###","#P#","###"]` → solids 에 위/아래 가로줄 병합된 사각형 포함, playerStart = (16,16).
- [ ] **5.** `js/player.js`: 중력 + 좌우 이동(가속/마찰) + 가변 점프(jump 키 홀드 시간으로 높이) + 최대 낙하속도 클램프.
  한쪽 통행 발판: `vy > 0` 이고 이전 프레임 발밑이 발판 위였을 때만 착지. 좌우 벽 충돌. `BB.util.wrapY` 로 위아래 래핑.
- [ ] **6.** `js/levels.js`: 테스트 레벨 1개.
- [ ] **7.** `main.js` PLAYING: 레벨 로드 + 주인공 생성, `update`/`render` 연결. 주인공은 `주인공1~5.png` 로 걷기 애니(속도 있으면 프레임 순환, 멈추면 1번).
- [ ] **8.** **사용자 기여:** `CONFIG.GRAVITY`, `CONFIG.JUMP_V`, `CONFIG.MAX_FALL` 값을 사용자가 정하도록 요청(추천값 함께 제시). 넣고 플레이 느낌 확인.
- [ ] **9.** 커밋 `feat: 지형 파서 + 주인공 이동/점프/중력`.

**직접 확인:** 주인공이 발판 위를 걷고, 점프로 위 발판에 올라가고(아래에서 통과), 화면 아래로 떨어지면 위에서 나온다. 좌우 끝에서 막힌다.

---

## M3: 거품 (뿜기 → 정지 → 상승 → 소멸, 거품 타기)

**파일:**
- 생성: `js/bubble.js`
- 수정: `js/player.js` (거품 뿜기 입력 + 거품 위 올라타기), `js/main.js`, `index.html`

**인터페이스:**
- `BB.Bubble` 클래스: `new BB.Bubble(x,y,dir, kind='normal')`. `kind`: `'normal'|'water'|'lightning'|'fire'`.
  `.update(dt, level)`, `.draw(ctx)`, `.state` (`'shoot'|'float'|'rise'|'pop'`), `.trapped` (적 참조 또는 null), `.dead` (제거 플래그), `.box()`.
- `BB.bubbles` — 현재 살아있는 거품 배열 (main.js 소유). 주인공/적이 참조.
- `BB.Player` 추가: `.bubbleCooldown`, 거품 위에 서면 `onGround`처럼 처리 + 살짝 위로 튕김.

- [ ] **1.** `js/bubble.js`: 상태머신.
  - `shoot`: `dir` 방향 빠른 속도, 약 0.4초 후 `float`.
  - `float→rise`: 잠깐 멈췄다 위로 부상. 천장/발판 밑면에서 옆으로 미끄러짐.
  - 8초 후 `pop`(1~2프레임 `방울3.png`) → `dead`.
  - `normal` 스킨 `방울1.png`.
- [ ] **2.** `player.js`: `bubble` 키 `pressed` + 쿨다운(0.3초) → `BB.bubbles.push(new BB.Bubble(...))`. 화면 내 내 거품 6개 제한.
- [ ] **3.** `player.js`: 거품 타기 — 발밑에 `normal` 이고 `trapped===null` 인 거품이 있으면 착지 + `vy = -작은값`(통통).
- [ ] **4.** `main.js`: `BB.bubbles` 배열 update/draw, `dead` 정리.
- [ ] **5.** 커밋 `feat: 거품 생성/이동/소멸 + 거품 타고 오르기`.

**직접 확인:** Space로 거품이 앞으로 날아가 멈췄다가 위로 떠올라 사라진다. 거품을 밟고 통통 튀며 위로 올라갈 수 있다.

---

## M4: 통통이 + 가두기 + 터뜨리기 + 과일 + 판 클리어

**파일:**
- 생성: `js/enemy.js`, `js/item.js`
- 수정: `js/bubble.js` (적과 충돌 시 가두기), `js/player.js` (갇힌 거품 부딪히면 터뜨리기 / 적 닿으면 죽음), `js/main.js`, `js/levels.js`

**인터페이스:**
- `BB.Enemy` 클래스: `new BB.Enemy(type,x,y)`. type `'walker'`(통통이). `.update(dt,level,player)`, `.draw`, `.state`(`'walk'|'trapped'|'dying'`), `.angry`, `.box()`, `.dead`.
- `BB.enemies` — 배열. `BB.enemies.length===0 && 스폰 끝` → 판 클리어.
- `BB.Fruit` 클래스: `new BB.Fruit(x,y,tier)`. tier 1..6 → 점수/스킨. `.update`(6초 후 소멸), `.draw`, `.dead`.
- `BB.fruits` — 배열.
- `BB.spawnFruitFromChain(bubbles)` — 연쇄로 터진 갇힌 거품 목록 → 위치별 Fruit 생성(개수→tier).

- [ ] **1.** `js/enemy.js`: 통통이 AI — 발판 위 좌우 순찰, 끝에서 방향전환, 가끔 낮은 점프, 위아래 래핑. `적1~4.png` 걷기 애니.
- [ ] **2.** `bubble.js`: `shoot`/`float`/`rise` 상태에서 `walk` 적과 `aabb` → 적 `state='trapped'`, 거품 `.trapped=적`, 스킨 `방울2.png`. 적은 거품 따라 위로. 5초 후 적 탈출(`state='walk'`, `angry` 잠깐).
- [ ] **3.** `player.js`: `trapped` 거품과 `aabb` → 그 거품 `pop` + 연쇄(닿아있는 다른 `trapped` 거품도) → `적소멸1~3.png` 애니 → `BB.spawnFruitFromChain`.
- [ ] **4.** `player.js`: `walk`/`angry` 적 또는 투사체와 `aabb` + 무적아님 → 주인공 죽음 → `BB.game.setState('MISS')`.
- [ ] **5.** `js/item.js`: `BB.Fruit` + 점수표(설계 5번) + 줍기.
- [ ] **6.** `main.js`: enemies/fruits update/draw, 판 클리어 판정 → `ROUND_CLEAR`(지금은 즉시 다음 판), MISS → 목숨 처리.
- [ ] **7.** `levels.js`: 통통이만 있는 판 2~3개로 교체.
- [ ] **8.** 커밋 `feat: 통통이 + 거품 가두기 + 터뜨려 과일 + 판 클리어`.

**직접 확인:** 거품으로 통통이를 가두고, 부딪혀 터뜨리면 과일이 나오고, 먹으면 점수가 오른다. 적을 다 잡으면 다음 판. 적에 닿으면 죽는다.

---

## M5: 적 3종 + 화난 적 + 심술고래

**파일:**
- 수정: `js/enemy.js` (type 추가, angry 로직, Whale), `js/levels.js`, `js/main.js`

**인터페이스:**
- `BB.Enemy` type 확대: `'walker'|'thrower'|'flyer'|'ghost'`. `thrower` 는 `BB.Projectile` 발사.
- `BB.Projectile` 클래스: `new BB.Projectile(x,y,vx,vy)`, `.update`, `.draw`, `.box()`, `.dead`. 주인공에 닿으면 죽음.
- `BB.Whale` 클래스: `new BB.Whale()` (화면 밖에서 등장), `.update(dt,player)` 무적·벽통과·직선추적, `.draw`, `.box()`. 판 클리어 시 제거.
- `BB.Enemy.setAngry(reason)` — tint + 속도/점프 증가.

- [ ] **1.** `enemy.js`: `thrower`(초록 hue, 멈춰서 수평 투사체), `flyer`(분홍, 대각선 비행+튕김, 발판 무시), `ghost`(보라, 발판 통과, 주인공 느린 추적). 색조는 오프스크린 캔버스로 hue-rotate 후 캐시.
- [ ] **2.** `enemy.js`: angry 트리거 — 적 1마리 남음 / 시간 20% 이하 / 거품 탈출 직후.
- [ ] **3.** `enemy.js`: `BB.Whale` — 판 타이머 0 → 등장. 픽셀 고래는 코드 도형으로 draw.
- [ ] **4.** `main.js`: 타이머 감소, 0에서 Whale 스폰, projectiles 배열 관리.
- [ ] **5.** `levels.js`: 각 적 종류가 나오는 판 추가.
- [ ] **6.** 커밋 `feat: 뿔몬/날개몬/유령몬 + 화난 적 + 심술고래`.

**직접 확인:** 4종류 적이 각자 다르게 움직인다. 마지막 1마리는 빨개지고 빨라진다. 오래 꾸물대면 고래가 나와 쫓아오고 닿으면 죽는다.

---

## M6: HUD + 화면들 + 콤보 점수 연출

**파일:**
- 생성: `js/hud.js`
- 수정: `js/main.js` (상태별 화면 그리기 hud.js로 위임), `index.html`

**인터페이스:**
- `BB.font.draw(ctx, text, x, y, opts)` — 픽셀 비트맵 폰트(코드로 5x7 글리프 정의, 대문자+숫자+기호). `opts.scale`, `opts.color`.
- `BB.hud.drawPlaying(ctx)` — 1UP/HIGH/ROUND, 목숨 공룡머리 아이콘, 시간 막대, EXTEND 글자.
- `BB.hud.drawTitle/drawRoundClear/drawMiss/drawGameOver/drawEnding(ctx, t)` — `t` 는 상태 진입 후 경과 시간.
- `BB.game` 확장: `hiScore` localStorage 저장/로드, `addScore(n)` (3만점마다 목숨+1), `roundFruitScore` (ROUND_CLEAR 합산 연출용).

- [ ] **1.** `js/hud.js`: 5x7 픽셀 폰트 글리프 테이블 + `BB.font.draw`.
- [ ] **2.** `hud.js`: `drawPlaying` — 상단 점수/최고/라운드, 하단 목숨 아이콘(`주인공1.png` 축소 or 코드 도형), 시간 막대(20%↓ 빨강+`HURRY!` 깜빡).
- [ ] **3.** `hud.js`: TITLE(로고 텍스트+PUSH SPACE+조작법+최고점수), ROUND_CLEAR(`ROUND CLEAR!`+과일점수 또르륵 합산 2초), MISS(`MISS!` 번쩍 1.5초), GAME_OVER(`GAME OVER`+최종점수+`계속하려면 클릭`+9→0 카운트다운), ENDING(축하+총점+크레딧).
- [ ] **4.** `main.js`: 각 상태에서 hud 호출, 상태 타이머 관리, 전환 로직 완성(MISS→목숨 남으면 같은 판 재시작 / 0이면 GAME_OVER, 마지막 판 클리어→ENDING).
- [ ] **5.** `game`: hiScore localStorage(try/catch), addScore.
- [ ] **6.** 커밋 `feat: 아케이드 HUD + 시작/클리어/미스/게임오버/엔딩 화면`.

**직접 확인:** 상단에 1UP·HIGH·ROUND, 하단에 목숨 아이콘·시간 막대. 판 클리어 시 과일 점수가 합산되는 연출. 죽으면 MISS, 목숨 0이면 GAME OVER 카운트다운. 최고점수가 새로고침 후에도 남는다.

---

## M7: 아이템 + EXTEND 글자

**파일:**
- 수정: `js/item.js` (PowerUp, ExtendLetter), `js/player.js` (효과 적용), `js/main.js`, `js/hud.js` (EXTEND 표시)

**인터페이스:**
- `BB.PowerUp` 클래스: `new BB.PowerUp(x,y,kind)`. kind `'shoe'|'candyYellow'|'candyRed'|'candyBlue'|'umbrella'|'potion'|'score'`. `.update`(발판 위 굴러다님), `.draw`, `.dead`.
- `BB.ExtendLetter` 클래스: `new BB.ExtendLetter(x,y,ch)` ch ∈ `E X T E N D`. 위로 떠오름, 주우면 `BB.game.extend[ch]=true`.
- `BB.Player` 확장: `.stats` (`speed`, `bubbleRange`, `bubbleCooldown`, `bubbleRise`) — 아이템으로 변경, 죽으면 리셋.
- `BB.game.extend` — `{E,X,T,E2,N,D}` 획득 플래그. 6개 다 차면 목숨+1 + 다음 판 + 초기화.

- [ ] **1.** `item.js`: `BB.PowerUp` 7종 + 효과 함수. 드랍 조건: 콤보 2+ 또는 처치 수 카운터 또는 낮은 랜덤.
- [ ] **2.** `player.js`: `stats` 적용(이동속도/거품 사거리/쿨다운/부상속도), 죽을 때 리셋. umbrella=판 점프, potion=과일 쏟기.
- [ ] **3.** `item.js`: `BB.ExtendLetter` + `main.js` 에서 갇힌 적 터뜨릴 때 낮은 확률 스폰.
- [ ] **4.** `hud.js`: 상단에 `E X T E N D` 획득 표시.
- [ ] **5.** `game`: 6글자 완성 처리.
- [ ] **6.** 커밋 `feat: 파워업 아이템 + EXTEND 글자`.

**직접 확인:** 가끔 신발·사탕이 떨어지고 먹으면 빨라지거나 거품이 세진다. EXTEND 글자를 다 모으면 목숨이 늘고 다음 판으로 간다.

---

## M8: 레벨 30판+ + 보스

**파일:**
- 수정: `js/levels.js` (전체 판 데이터), `js/enemy.js` (Boss), `js/main.js` (보스 판 분기), `js/level.js` (테마 색 추가)

**인터페이스:**
- `BB.LEVELS` — 30개+ 배열. 각 `{time, theme, map:[32칸 x 24줄]}`.
- `BB.Boss` 클래스: `new BB.Boss(x,y)`. `.hp`, `.update(dt,level,player,bubbles)`, `.draw`, 거품에 가뒀다 터뜨리면 hp-1, 투사체·졸개 소환. hp 0 → 판 클리어 → ENDING.
- `BB.THEMES` — `{ pink:{...색}, sky:{...}, lime:{...}, ... }`.

- [ ] **1.** `level.js`: `BB.THEMES` 10테마 색.
- [ ] **2.** `levels.js`: 1판 = `지형.png` 배치를 32x24 글자맵으로 옮김(스크린샷 참고). 통통이 2~3.
- [ ] **3.** `levels.js`: 2~10판(대칭 쉬움), 11~20판(뿔몬·날개몬), 21~29판(유령몬·좁은통로). 한 판씩 만들고 플레이 확인.
- [ ] **4.** **사용자 기여:** 7판 글자 지도를 사용자가 그리게 요청(빈 32x24 틀 + 예시 제공).
- [ ] **5.** `enemy.js`: `BB.Boss` (확대 스프라이트, hp 8~10, 패턴 2~3개).
- [ ] **6.** `levels.js`/`main.js`: 30판 = 보스 무대.
- [ ] **7.** 커밋 `feat: 30판 레벨 데이터 + 보스`.

**직접 확인:** 1판이 원작 1스테이지 모양. 판이 갈수록 어려워지고 10판마다 색이 바뀐다. 30판에서 보스와 싸워 이기면 엔딩이 나온다.

---

## M9: 특수 거품 + 소리

**파일:**
- 생성: `js/sfx.js`
- 수정: `js/bubble.js` (water/lightning/fire 효과), `js/main.js`, `js/hud.js` (음소거 표시), `index.html`

**인터페이스:**
- `BB.sfx.play(name)` — name: `blow, pop, jump, land, trap, enemyPop, fruit, item, extend, combo, miss, roundClear, whale, bossHit`. WebAudio 오실레이터+엔벨로프.
- `BB.sfx.music.start()/stop()/toggleMute()` — `배경음악.mp3` 루프.
- `BB.sfx.muted` — localStorage 저장.

- [ ] **1.** `js/sfx.js`: WebAudio 컨텍스트(첫 입력 후 resume), 각 효과음 함수, 배경음악 Audio 루프.
- [ ] **2.** 게임 곳곳에 `BB.sfx.play(...)` 연결.
- [ ] **3.** `bubble.js`: 특수 거품 3종 효과(물줄기/번개/불기둥) + 생성 확률(낮음).
- [ ] **4.** `input`/`hud`: `M` 음소거 토글 + 표시.
- [ ] **5.** (선택) freesound.org CC0 효과음 몇 개 `assets/sfx/` 추가 + `README` 에 출처.
- [ ] **6.** 커밋 `feat: 특수 거품 + WebAudio 효과음 + 배경음악`.

**직접 확인:** 거품 뿜기·터지기·점프·과일 소리가 난다. 배경음악이 흐르고 M으로 끌 수 있다. 가끔 물·번개·불 거품이 나온다.

---

## M10: 다듬기 + 배포

**파일:**
- 수정: 전체, `README.md`
- 생성: (나중에) `CNAME`

- [ ] **1.** 밸런스 패스: 판별 시간, 적 수, 아이템 확률, 점프 느낌 최종 조정(사용자 플레이테스트 반영).
- [ ] **2.** 일시정지(P), 첫 화면 조작 안내, 모바일 터치는 범위 밖(README에 "키보드 필요" 명시).
- [ ] **3.** `test.html` 최종 통과 확인. 주요 브라우저(크롬) 콘솔 에러 0.
- [ ] **4.** playwright로 각 화면 스크린샷 찍어 사용자에게 전달.
- [ ] **5.** `README.md` 완성(플레이 링크 자리, 조작법, 크레딧, 고지).
- [ ] **6.** 배포:
  ```bash
  gh repo create bubble-bobble-clone --public --source=. --remote=origin --description "팬이 공부용으로 만든 버블보블 스타일 웹 게임 (비상업·학습용)"
  git push -u origin main
  gh api -X POST repos/{owner}/bubble-bobble-clone/pages -f "source[branch]=main" -f "source[path]=/" || (Settings→Pages 수동 안내)
  ```
- [ ] **7.** 배포 URL 확인 → `README.md` 링크 채우고 커밋/푸시.
- [ ] **8.** (나중에) 사용자가 커스텀 도메인 알려주면: `CNAME` 파일 생성 + `gh` 로 Pages CNAME 설정 + DNS(A레코드/CNAME) 안내.

**직접 확인:** 인터넷 주소로 접속해서 게임이 돌아간다. README에 크레딧과 고지가 있다.

---

## 자체 점검 (계획 ↔ 설계)

- 설계 2~10 각 항목이 M1~M10에 매핑됨: 화면/루프=M1, 이동=M2, 거품=M3·M9, 적=M4·M5, HUD/화면=M6, 아이템=M7, 레벨=M8, 소리=M9, 배포=M10. ✅
- 설계 13(2인 플레이 제외, 스프라이트는 코드도형 우선, Space겸용은 플레이테스트) → 계획에 반영(범위 밖 명시, M6/M8에서 코드도형, M10 밸런스). ✅
- 타입 이름 일관성: `BB.Bubble.trapped`, `BB.enemies`, `BB.fruits`, `BB.bubbles`, `BB.game.extend` — M3~M8에서 동일하게 사용. ✅
- 테스트: 순수 로직(parseLevel, aabb, 점수표)만 `test.html` 로 검사. 나머지는 "직접 확인" 목록으로 수동 검증(캔버스 게임 특성상). 명시됨. ✅
